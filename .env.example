# Jerri Finance Portal deliberately has no cloud credentials or remote URL.
# The server binds to localhost by default. JERRI_PORT is optional when the
# default local address (http://127.0.0.1:4747) is already in use.
JERRI_PORT=4747
